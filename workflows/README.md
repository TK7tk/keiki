## このフォルダについて

この `workflows/` は、`rules/` のルールに沿って **VOICE→ブログmd** を生成する「手順（運用）」を置く場所です。

### 最短の使い方
- **普段はコピペ不要**です（`@`参照でOK）。
- `workflows/quick_run.md` を開き、1行の指示で実行
- うまくいかない時だけ `workflows/blog_from_voice.md` の「実行テンプレ（長文）」を使う

